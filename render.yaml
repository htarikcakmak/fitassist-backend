# Render.com için Deployment Ayar Dosyası
services:
  - type: web
    name: fitassist-backend
    env: java
    plan: free 
    buildCommand: "mvn clean package -DskipTests" 
    startCommand: "java -jar target/demo-0.0.1-SNAPSHOT.jar" 
    envVars:
      - key: PORT
        value: 8080 